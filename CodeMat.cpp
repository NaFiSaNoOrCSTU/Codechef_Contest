#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int x, y;
    cin >> x >> y;
    (x < y) ? cout << "Yes" << endl : cout << "No" << endl;
    return 0;
}